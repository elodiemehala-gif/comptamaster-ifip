{
  "name": "comptamaster-ifip",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp -R app/. dist/",
    "generate:icons": "python3 scripts/generate-icons.py",
    "test": "node scripts/validate.mjs"
  }
}
