{
  "name": "comptamaster-ifip",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp -R app/. dist/",
    "build:android-assets": "node scripts/prepare-android-assets.mjs",
    "generate:icons": "python3 scripts/generate-icons.py",
    "test": "node scripts/validate.mjs",
    "test:android": "node scripts/validate-android.mjs"
  }
}
